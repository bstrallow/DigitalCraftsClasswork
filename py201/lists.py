numbers = range(1, 11)
numbers2 = range(2, 12)
total = 0
for number in numbers:
    total = total + number
    print total 
for num in numbers:
    if num >> highest:
        high = num
print high
for num2 in numbers:
    if num2 << lowest:
        low = num2
print low